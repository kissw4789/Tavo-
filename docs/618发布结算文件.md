# 618 发布结算文件

结算时间：2026-06-18

覆盖范围：从 11:06 之后落地到 `C:\Users\QQQ\Desktop\OH-WorkSpace\_json_work\我的插件包6-17_v088` 的提取代码改动，并统一注入到发布版 JSON。

发布文件：

- `C:\Users\QQQ\Desktop\OH-WorkSpace\我的插件包6-17_v088_618发布版.json`

## 一、改动总览

本次发布包包含三类改动：

1. 0.88 底层收口：存储 fallback、记忆世界书自动绑定、旧数据迁移入口、帮助入口文案统一、关系降级提示。
2. 记忆系统待处理页优化：待处理/已完成/全部筛选，完成项不再因更新时间被顶到待处理前面。
3. 关系世界书重写版落地：输出规则、阶段指引、演绎总则、成熟度提示更新，并修正核心条目最强注入字段。

## 二、文件级修改记录

### `code_3.js`

- 保留 0.88 底层逻辑版中对旧存储/平台 API 的兼容修复。
- 发布前检查未命中旧错误文案 `platformCore.rawSet 不可用`。

### `code_4.js`

- 回忆系统帮助入口文案从“新人必看”统一为“使用说明”，已读后显示“帮助”。
- 发布前检查未命中“新人必看”残留。

### `code_5.js`

- 保留平台 API fallback 相关修复。
- 发布前检查未命中旧 `rawSet` 报错文案。

### `code_6.js`

- 新增 `ui.pendingFilter:'pending'`，默认进入“待处理”视图。
- 对 `pendingFilter` 做合法化，只允许 `pending / done / all`。
- 保留记忆世界书自动绑定逻辑：启动、同步、总开关开关、清空时按状态绑定或解绑。
- 保留 TAV 0.88 迁移入口：
  - `tav088UpgradeStatus`
  - `migrateOldMemoToTav088`
  - `rebuildMemoFromFirstFloorTav088`
- 迁移旧数据为本地转换，不消耗 API；从第一楼重新生成会走历史补录，会消耗 API。

### `code_7.js`

- 待处理页新增顶部筛选栏：
  - `待处理`
  - `已完成`
  - `全部`
- 完成项和待处理项分区渲染：
  - 默认只显示待处理。
  - 完成项进入“已完成”视图，不再因为 `updatedAt` 更新被顶到待处理顶部。
  - “全部”视图中先显示待处理，再显示已完成。
- 复用现有 `tm8-memory-card` 样式，只新增轻量筛选按钮与完成态样式。
- 完成态卡片增加淡化和标题划线。
- 标记完成/重新打开后自动收起当前卡片。
- 帮助入口文案为“使用说明”，已读后显示“帮助”。

### `code_9.js`

- 关系阶段标准统一为：
  - `陌路 / 初识 / 往来 / 纠葛 / 羁绊 / 宿命`
- 阶段范围：
  - 陌路 `0-4`
  - 初识 `5-14`
  - 往来 `15-39`
  - 纠葛 `40-64`
  - 羁绊 `65-89`
  - 宿命 `90-100`
- `normCharState()` 按 `depth` 推导阶段，避免旧存储阶段名污染。
- 世界书阶段条目不再重复创建，运行时会清理 `rel_stage_*` 条目。
- 保留旧阶段名映射，只用于识别和清理旧条目，不作为新标准。
- `buildRulesContent()` 已替换为关系世界书重写版：
  - 新增 `interactions` 字段。
  - 输出模板移除旧 `pressureReports`。
  - 事件证据压缩到更明确的长度约束。
- `buildStageContent()` 已替换为六阶段新版指引。
- `buildGeneralGuideline()` 已替换为新版演绎总则。
- `buildStageBriefContent()` 与 `MATURITY_HINT` 已按新版更新。
- 修正核心世界书注入强度：
  - `injectionPosition:'atDepth'`
  - `injectionDepth:0`
  - `injectionRole:'system'`
- 修正原先 `Number(opts.injectionDepth||4)||4` 会把 `0` 变成 `4` 的问题。
- `rel_rules / rel_general_guideline / rel_event_types` 现在会强制更新内容，避免旧世界书条目已有内容时跳过新版。
- 新增 `interactions` 解析：
  - 能解析角色 A → 角色 B 的互动信号。
  - 写入 `recentInteractions` 和协议日志。
  - 角色名无法识别时进入待确认。
  - 不把角色间互动偷算成用户与角色的好感/信任/深度变化。
- 关系降级原因写入结算结果和日志，供 UI 展示。

### `code_10.js`

- 关系系统帮助入口文案为“使用说明”，已读后显示“帮助”。
- 帮助已读状态读写全局键 `fifth_season_relation_help_seen`。
- 关系变化反馈中展示降级原因，避免用户只看到“降级”但不知道原因。

## 三、复查结论

已确认到位：

- 待处理页默认显示待处理，不再让完成项顶到前面。
- 完成项可以从“已完成”和“全部”视图查看。
- 关系世界书核心条目使用 `atDepth + 0 + system`。
- `injectionDepth:0` 不再被误修正成 `4`。
- `rel_stage_*` 阶段世界书条目不会再被创建，运行时会清理旧条目。
- 旧阶段名仅保留为迁移/识别映射，不作为新阶段标准。
- `interactions` 不会再被静默忽略。
- `新人必看` 文案无残留。
- `platformCore.rawSet 不可用` 旧报错文案无残留。

保留边界：

- `interactions` 当前是“角色间互动记录”，不是完整角色关系网结算。它不会改用户与角色的关系值，这是有意保守处理，避免错误串账。
- 关系世界书已有旧条目的用户，需要触发关系世界书检查/更新流程后，运行时才会清理旧 `rel_stage_*` 条目并写入新版核心内容。
- 正则脚本数组原始就是 14 个槽位、12 个非空脚本，本次发布保持完全一致，没有新增或删除槽位。

## 四、验证记录

已执行：

- 提取目录检查：通过
  - `workflow.py check _json_work\我的插件包6-17_v088`
- 注入发布版 JSON：通过
  - 输出：`我的插件包6-17_v088_618发布版.json`
  - JSON parse：OK
  - replaceString fields：12
- 深度验证：通过
  - 原始 JSON 合法
  - 发布 JSON 合法
  - 顶层结构完全一致
  - key/数组长度无变化
- 正则槽位计数：
  - 原始：`regex_scripts=14`，非空 `replaceString=12`
  - 发布版：`regex_scripts=14`，非空 `replaceString=12`
  - 空槽位均为 `[11, 12]`
- 发布包内容检查：
  - `pendingFilter`：存在
  - `pending-filter`：存在
  - `interactions`：存在
  - `recentInteractions`：存在
  - 强注入字段：存在
  - `platformCore.rawSet 不可用`：无
  - `新人必看`：无
  - 旧输出模板：无

## 五、发布判断

可以发布。

这版不是继续试验稿，已经完成本次 11:06 之后的改动收口、结构验证和发布包生成。剩余的多角色完整模板、关系网二次结算、关系世界书具体长文案优化，可以作为后续版本继续做，不影响本次发布。
